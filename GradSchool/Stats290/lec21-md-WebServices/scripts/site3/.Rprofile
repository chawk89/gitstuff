library(distcomp)
dcWorkspace <- "~/distcomp/site3/workspace"
if (! file.exists(dcWorkspace) ) {
    ## create it
    dir.create(dcWorkspace)
}
distcompSetup(workspace = dcWorkspace,
              ssl_verifyhost = 0L, ssl_verifypeer = 0L)
